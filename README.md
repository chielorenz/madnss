# Madnss
Static site from markdown withoud madness

## Usage
Create a folder named `pages/` and add some `.md` files in it. Run `node main.js` and check out the result in the `public/` folder.

## Roadmap
- try out markdown-it and try to convert a simple md to html
- try to convert multiple files from /pages to /public
- figure out how to serve the public/ folder
- add markdown-it-attrs to add classes
- add markdown-it-frtont-matter to update the template 
- implement partials/